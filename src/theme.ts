import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#AECCCD',
      main: 'rgba(47,225,57,1)',
      dark: '#219195',
      contrastText: '#fff',
    },
  },
});

export default theme;