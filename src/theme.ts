import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#AECCCD',
      main: '#29a130',
      dark: '#39ca23',
      contrastText: '#000000',
    },
  },
});

export default theme;